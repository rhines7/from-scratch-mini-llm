# Raw data (see data/README.md for how to obtain)
data/tinystories/
data/emotions_classified.csv

# Model checkpoints and large weights (regenerated by training)
checkpoints/
sentiment_checkpoints/
*.pt

# Regenerated tokenizer vocabulary (rebuilt during data prep)
qwen3_tokenizer_vocab.json

# Raw training logs (summaries are committed under artifacts/)
logs/

# Pipeline caches; keep committed summary JSON/CSV
artifacts/*
!artifacts/*.json
!artifacts/*.csv
*.npy
*.npz

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/

# Jupyter
.ipynb_checkpoints/
*_executed.ipynb

# LaTeX build artifacts
docs/*.aux
docs/*.log
docs/*.out
docs/*.synctex.gz

# OS
.DS_Store
Thumbs.db
